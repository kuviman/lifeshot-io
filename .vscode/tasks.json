{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Local run",
            "type": "shell",
            "command": "cargo run --release -- --host 127.0.0.1 --port 1234 --addr ws://127.0.0.1:1234 --log-level debug with-server",
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}