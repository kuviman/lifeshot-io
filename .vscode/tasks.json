{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Local run",
            "type": "shell",
            "command": "cargo run --release -- --host 127.0.0.1 with-server",
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}